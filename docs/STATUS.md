# Build status

The NEXORA foundation is implemented on `build/nexora-mvp`.

Supabase is provisioned and verified with the core schema, starter content and RLS policies. The GitHub repository contains the Next.js application shell, platform screens, Supabase SSR auth utilities, CI build workflow and Vercel configuration.
