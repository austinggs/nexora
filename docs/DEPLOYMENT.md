# Deployment

Vercel can connect directly to the GitHub repository. Once imported, pushes and pull requests can receive preview deployments, while merges to `main` can produce production deployments.

Required public environment variables are documented in `.env.example`.
