# QA targets

Use the supplied test strategy as the acceptance target: unit coverage, integration coverage, WebSocket command coverage, load testing at 5,000 concurrent users, Playwright critical journeys, and OWASP/manual review of high-risk endpoints.
