import Link from 'next/link'
import { ArrowRight, CircleDollarSign, Gamepad2, ShieldCheck, Users } from 'lucide-react'

export default function LandingPage() {
  return <main style={{minHeight:'100vh',padding:'28px clamp(18px,5vw,72px)'}}><header style={{display:'flex',justifyContent:'space-between',alignItems:'center'}}><Link className="brand" href="/"><span className="brand-mark"/>NEXORA</Link><Link className="btn secondary" href="/login">Sign in</Link></header><section style={{maxWidth:980,margin:'11vh auto 0',textAlign:'center'}}><div className="eyebrow">Social discovery · verified rewards · mining game</div><h1 style={{fontSize:'clamp(44px,8vw,86px)',lineHeight:.95}}>Build a better<br/><span style={{color:'var(--accent-2)'}}>digital economy.</span></h1><p className="muted" style={{maxWidth:720,margin:'26px auto',fontSize:16,lineHeight:1.8}}>NEXORA brings conversations, legitimate earning opportunities and a virtual PC mining game into one trust-first platform.</p><div style={{display:'flex',justifyContent:'center',gap:10,flexWrap:'wrap'}}><Link className="btn" href="/app">Enter NEXORA <ArrowRight size={16} style={{verticalAlign:'middle'}}/></Link><Link className="btn secondary" href="/login">Create account</Link></div></section><section style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(220px,1fr))',gap:14,maxWidth:980,margin:'10vh auto 0'}}>{[[Users,'Community','Threads, topics and personalized discovery'],[CircleDollarSign,'Verified rewards','Clear opportunity states and ledger-backed balances'],[Gamepad2,'Mining game','Build rigs, manage thermals and join guild events'],[ShieldCheck,'Trust by design','Secure sessions, RLS and auditable financial actions']].map(([Icon,title,copy])=><div className="glass section" key={title as string}><Icon size={21}/><h3>{title as string}</h3><p className="muted" style={{fontSize:13,lineHeight:1.6}}>{copy as string}</p></div>)}</section></main>
}
