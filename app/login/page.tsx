import { login, signup } from './actions'

export default function LoginPage() {
  return <main className="login-shell"><section className="glass login-card"><div className="brand" style={{padding:'0 0 22px'}}><span className="brand-mark"/>NEXORA</div><div className="eyebrow">Secure access</div><h1 style={{fontSize:32,marginBottom:8}}>Welcome back.</h1><p className="muted" style={{lineHeight:1.6}}>Use email + password. Sessions are stored in secure HTTP-only cookies; credentials never live in localStorage.</p><form><div className="field"><label htmlFor="email">Email</label><input id="email" name="email" type="email" required autoComplete="email"/></div><div className="field"><label htmlFor="password">Password</label><input id="password" name="password" type="password" required minLength={8} autoComplete="current-password"/></div><div style={{display:'grid',gap:10,marginTop:18}}><button className="btn" formAction={login}>Log in</button><button className="btn secondary" formAction={signup}>Create account</button></div></form><p className="footer-note">By continuing, you agree to use NEXORA responsibly. Financial outcomes are not guaranteed and verified opportunities are clearly labeled.</p></section></main>
}
